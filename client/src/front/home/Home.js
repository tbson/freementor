// @flow
import * as React from 'react';

type Props = {
};
type State = {
};

export default class Home extends React.Component<Props, State> {
    static defaultProps = {};
    state: State = {
    };

    render() {
        return (
            <h1>
                Front page
            </h1>
        );
    }
}
